# Local machine environment preset (copy from environment.example)
/environment

# Migrate backups (may contain old dotfiles with secrets)
backups/

# Ontology extract output (generated by bin/extract-ontology-facts.sh)
.agents/ontology/shell-kernel.extracted.yaml

# Optional: uncomment to keep personal overlay out of git
# local/personal.sh
